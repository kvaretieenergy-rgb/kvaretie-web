/* KVARETIE · Social links (v3, 2026-09-26).
 * Intentionally does NOT intercept any link. The Instagram, Facebook, TikTok and WhatsApp
 * buttons are plain HTTPS links in the HTML and open directly:
 *  - In Safari / Chrome the phone hands tiktok.com links to the TikTok app if it is installed.
 *  - Inside the Instagram / Facebook in-app browsers the profile opens in that browser;
 *    Meta decides whether the TikTok app can be launched, and a web page cannot change that.
 * v1/v2 showed a panel with extra steps (open in browser / copy link); it was removed at the
 * owner's request because it added steps. This file is kept so pages that load it stay valid. */
